---
title: Cross-page checks
---

(sec-cross-page)=
## References survive page boundaries

This page refers back to @eq-send-more-money, @fig-search-tree, and
@code-search-loop. It also links to [](#sec-first-model) by its stable label and
to @table-results. The same shared macro still renders as $\Model$, and the typed
API role again resolves {gecode-api}`class:Gecode::Space` from the release
inventory.

The important invariant is independent of the renderer: the included excerpt and
the downloadable program both originate in `examples/send-more-money.cpp`.
